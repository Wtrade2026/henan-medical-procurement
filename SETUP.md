# 河南医疗设备招投标采集 — 安装与使用指南

## 第一步：安装 Python

1. 打开 https://www.python.org/downloads/
2. 点击黄色按钮 **Download Python** 下载安装包
3. 双击运行安装包
4. ⚠️ **务必勾选底部的 "Add Python to PATH"**（勾上之后再点 Install Now）

验证安装：按 `Win+R`，输入 `cmd` 回车，输入以下命令：
```
python --version
```
如果显示 `Python 3.x.x`，说明安装成功。

## 第二步：下载程序

1. 打开 https://github.com/Wtrade2026/henan-medical-procurement
2. 点击绿色 **Code** 按钮 → **Download ZIP**
3. 解压 ZIP 文件到任意位置（建议桌面）

## 第三步：安装依赖（仅第一次）

进入解压后的文件夹，双击运行 `install.bat`。

或者手动打开命令行执行：
```
pip install -r requirements.txt
```

首次运行时会自动下载验证码识别模型（约 100MB），请耐心等待。

## 第四步：确认你的配置文件

每个人负责不同的数据源，配置文件已经预先写好，一般不需要修改。

进入你的文件夹（`person-a` / `person-b` / `person-c`），打开 `config.json` 确认：

- `time_range`: 时间范围
  - `0` = 仅今日
  - `1` = 近一周（推荐）
  - `2` = 近一月

如果要修改，用记事本打开编辑保存即可。

## 第五步：运行

在 **你的文件夹**（如 `person-b`）内：

**方法一（推荐）**：双击 `run.py`

**方法二**：在文件夹地址栏输入 `cmd` 回车，然后输入：
```
python run.py
```

程序会自动：
1. 打开网站、识别验证码
2. 翻页搜索所有公告
3. 筛选医疗设备相关
4. 归类到你的负责城市
5. 抓取详情正文
6. 保存到 `output/` 文件夹

## 第六步：查看结果

运行完毕后，进入 `output/` 文件夹，找到 `.json` 文件。这就是你采集的数据。

### 如何在 Excel 中打开 JSON？

1. 打开 Excel → 数据 → 获取数据 → 从文件 → 从 JSON
2. 选择你的 `.json` 文件
3. 点击"加载"即可看到表格

也可以直接拖到浏览器中查看。

## 常见问题

### pip 找不到？
Python 安装时没有勾选 "Add Python to PATH"。重新安装 Python 并勾选。

### "访问频繁"？
程序会自动等待，不要手动关闭。限流解除后自动继续。

### "验证码失败"？
偶尔的识别失败，程序会自动重试最多 15 次。如果一直失败，换个时间段再试。

### 运行很慢？
正常的。每个网站间隔 5 秒翻页，每个公告间隔 2 秒抓详情，这是为了不被网站封禁。完整运行约需 15-30 分钟。

### 需要代理吗？
如果在公司内网环境，可能需要配置代理。请联系 Wayne 获取代理设置。

## 技术支持

有问题联系 Wayne。
